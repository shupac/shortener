
<h2>Available Links</h2>
<ul class="links">
<% @links.each do |link| %>
  <li>
    <a href="/<%= link.shortened %>"><%= link.shortened %> </a><span><%= link.url %></span>
  </li>
<% end %>
</ul>

<a href="<%= url('/new') %>">Create new &rarr;</a>
